% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tableLayoutDualColumns.R
\name{tableLayoutDualColumns}
\alias{tableLayoutDualColumns}
\title{Create a table layout with dual columns for Shiny UI input elements.}
\usage{
tableLayoutDualColumns(
  labels,
  widgets,
  widgets2,
  headerLabels = c("Header 1", "Header 2"),
  firstColWidth = "30\%",
  fontSize = "16px",
  paddingBottom = "15px",
  isBold = TRUE,
  headerFontSize = "16px",
  headerIsBold = TRUE,
  headerAlignment = "center",
  removeFirstCol = FALSE,
  colPercents = c(50, 50),
  colSpacing = "10px"
)
}
\arguments{
\item{labels}{A vector of label names for the widgets.}

\item{widgets}{A list of Shiny UI input elements corresponding to the labels
(first set).}

\item{widgets2}{A list of Shiny UI input elements corresponding to the labels
(second set).}

\item{headerLabels}{A vector of two header labels for the table's first row.}

\item{firstColWidth}{The width of the first column (for the labels).}

\item{fontSize}{The font size for the label text.}

\item{paddingBottom}{The padding at the bottom of the label text.}

\item{isBold}{Logical; If TRUE, the label text is displayed in bold.}

\item{headerFontSize}{Font size for the table headers.}

\item{headerIsBold}{Logical; If TRUE, the header text is displayed in bold.}

\item{headerAlignment}{Alignment of the header text. Options: left, center, right.}

\item{removeFirstCol}{Logical; If TRUE, the first column (labels) will be
removed.}

\item{colPercents}{A numeric vector indicating the percentage width of each
column.}

\item{colSpacing}{Spacing between columns.}
}
\value{
A Shiny table layout with dual columns.
}
\description{
Create a table layout with dual columns for Shiny UI input elements.
}
\examples{
\dontrun{
library(shiny)

ui <- fluidPage(
  tableLayoutDualColumns(
    labels = c("Label 1", "Label 2", "Label 3"),
    widgets = list(
      textInput("input1", "Input 1", ""),
      sliderInput("input2", "Input 2", 1, 10, 5),
      selectInput("input3", "Input 3", choices = c("A", "B", "C"),
      selected = "A")
    ),
    widgets2 = list(
      textInput("input4", "Input 4", ""),
      sliderInput("input5", "Input 5", 1, 10, 5),
      selectInput("input6", "Input 6", choices = c("X", "Y", "Z"),
      selected = "X")
    ),
    headerLabels = c("Inputs Set 1", "Inputs Set 2"),
    firstColWidth = "20\%",
    fontSize = "14px",
    paddingBottom = "5px",
    isBold = TRUE,
    headerFontSize = "16px",
    headerIsBold = TRUE,
    headerAlignment = "center",
    removeFirstCol = FALSE,
    colPercents = c(50, 50),
    colSpacing = "10px"
  )
)
shinyApp(ui, function(input, output) {})
}

}
