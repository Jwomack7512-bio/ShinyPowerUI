% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResizable.R
\name{plotResizable}
\alias{plotResizable}
\title{Resizable Shiny Plot Output}
\usage{
plotResizable(
  plot_output,
  autoResizeWithWindow = TRUE,
  showBorderOnResize = TRUE
)
}
\arguments{
\item{plot_output}{A Shiny plot output element, typically from `plotOutput()`.}

\item{autoResizeWithWindow}{Logical. If `TRUE`, the plot resizes automatically with the window. Default is `TRUE`.}

\item{showBorderOnResize}{Logical. If `TRUE`, shows a dotted border around the plot while resizing. Default is `TRUE`.}
}
\value{
A Shiny UI element (HTML tags) that wraps the provided plot output, making it resizable.
}
\description{
Wraps a Shiny plot output in a resizable container.
}
\examples{
\dontrun{
library(shiny)

ui <- fluidPage(
  titlePanel("Resizable Plot Demo"),
  sidebarLayout(
    sidebarPanel(
      helpText("Resize the plot by dragging from its edges.")
    ),
    mainPanel(
      plotResizable(plotOutput("distPlot"))
    )
  )
)

server <- function(input, output) {
  output$distPlot <- renderPlot({
    hist(rnorm(100))
  })
}

shinyApp(ui = ui, server = server)
}

}
